# Напишите скрипт, который преобразует входную строку в верхний регистр.
# Например, 
#   'hello' -> 'HELLO'
# методы upper(), swapcase() использовать нельзя!

Можно использовать следующий код на Python:
def to_upper_case(s):
    result = ""
    for c in s:
        if 'a' <= c <= 'z':
            result += chr(ord(c) - 32)
        else:
            result += c
    return result
  Эта функция принимает на вход строку s и возвращает строку в верхнем регистре. Она проходится по каждому символу входной строки и, если символ является буквой в нижнем регистре, заменяет его эквивалентом в верхнем регистре. Если символ не является буквой в нижнем регистре, он добавляется в результат без изменений.
  
  
  или
  
def to_upper_case(s):
    Функция преобразует входную строку в верхний регистр.
    # создаем пустую строку для результата 
    result = ""
    # пройдемся по каждому символу во входной строке
    for c in s:
        # проверяем, является ли символ буквой в нижнем регистре
        if ord('a') <= ord(c) <= ord('z'):
            # если да, то добавляем к результату символ в верхнем регистре
            result += chr(ord(c) - ord('a') + ord('A'))
        else:
            # если нет, то добавляем к результату исходный символ
            result += c
    return result
